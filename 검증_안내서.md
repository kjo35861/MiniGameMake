# 검증 안내서 — NEON WARD

## 어디로 가나요
https://kjo35861.github.io/MiniGameMake/ 접속

## 무엇을 하나요
1. 접속하면 나오는 로비에서 캐릭터 캐러셀(◀ ▶)로 캐릭터를 확인한 뒤 **[게임 시작]** 버튼 클릭 (잠긴 캐릭터가 보이면 그대로 둬도 됨 — 기본 캐릭터로 시작됨)
2. 방향키(또는 WASD)로 3~5초 정도 움직이며 탄막 피하기 — 모바일/터치 환경에서는 게임 화면을 손가락으로 드래그해서 이동
3. 화면 아무 곳이나 클릭할 필요 없이, 하트가 줄거나(피격) 몬스터가 죽는 것(점수 증가)을 확인

## 무엇이 보이면 통과인가요
- 로비 화면에 캐릭터 캐러셀(인터셉터/레이저 발키리/드리프터), 최고 기록, `게임 시작` 버튼이 보인다
- 잠긴 캐릭터(레이저 발키리, 드리프터)는 🔒 표시와 함께 `게임 시작` 버튼이 `🔒 잠김`으로 바뀌며 선택할 수 없다
- `?` 버튼에 마우스를 올리거나(모바일은 탭) 규칙 설명 툴팁이 화면 안에서 뜬다(모바일에서는 화면 중앙에 고정 표시)
- 시작 후 3초 이내에 화면 상단 좌측 하트와 중앙 라운드 진행 바가 정상적으로 보인다 (인터셉터·레이저 발키리는 하트 3개, 드리프터는 2개)
- 방향키를 누르면 기체가 즉시 반응해서 움직이고, 공격은 캐릭터별로 자동 발사된다(확산탄/레이저 빔/유도 미사일)
- 몬스터에 닿거나 총알을 맞으면 하트가 하나 줄고, 하트가 0이 되면 "실패" 화면과 `다시 시작`·`처음부터`·`로비로` 버튼이 뜬다
- `처음부터`(또는 `다시 시작`)를 누르면 라운드 1부터 점수와 체력이 초기화된 새 판이 시작된다
- 라운드 진행률은 몬스터를 잡지 않아도 시간이 지나면 자동으로 오른다 (몬스터 처치와는 무관)
- 2라운드부터 노란 마름모 모양의 속사형 몬스터가 등장해 일정 시간마다 3발씩 끊어 쏜다
- 4라운드부터 남보라색 팔각형 탱커형 몬스터가 등장해 느리게 움직이며 굵은 탄막을 자주 뿌린다
- 몬스터를 처치하면 가끔 노란 폭탄 아이템이 떨어지고, 닿으면 화면의 적 탄이 전부 사라진다 (이 획득 횟수가 15회 쌓이면 드리프터가 해금된다)
- 진행률 50%에서 엘리트가, 100%에서 보스가 등장하며, 보스 등장 시 쫄몹이 함께 소환된다(최종 보스는 제외)
- 보스는 링/조준/십자/나선 등 여러 탄막 패턴을 번갈아 사용하며, 5라운드 이상부터는 예고선이 있는 직선 레이저도 쏜다
- 10라운드의 최종 보스는 보라색이 아닌 진홍색으로 표시되며, 처치하면 "GAME CLEAR" 엔딩 화면과 함께 레이저 발키리가 해금된다
- 우측 상단 `⏸` 버튼(진행 중일 때만 노출) 또는 `Esc` 키를 누르면 즉시 일시정지되고, `계속하기`를 누르거나 `Esc`를 다시 누르면 이어서 진행된다
- 모바일/터치 화면에서는 손가락으로 화면을 드래그하면 기체가 그 방향으로 자연스럽게 따라온다
- 세로로 든 모바일 화면에서는 게임 화면 자체가 세로형 해상도로 전환되어 화면을 크게 채운다 (가로 화면에서는 기존 가로형 그대로)

## 안될 때
- **화면이 하얗거나 아무것도 안 보임**: 브라우저 새로고침(F5) 후 재시도. 그래도 안 되면 다른 브라우저(Chrome 권장)로 시도
- **방향키를 눌러도 기체가 안 움직임**: 게임 화면(캔버스) 영역을 한 번 클릭해서 포커스를 준 뒤 다시 시도
- **탭을 전환했다가 돌아왔더니 멈춰 있음**: 정상 동작입니다 — 포커스 이탈 시 자동 일시정지되며, 화면의 `계속하기` 버튼이나 `Esc` 키를 누르면 이어서 진행됩니다
- **소리가 하나도 안 남**: 상단 `🔊 소리 끄기` 버튼이 눌려있는(음소거) 상태인지 확인
- **폭탄 아이템이 안 보임**: 확률 드롭이라 매 판마다 나오지 않을 수 있음. 몬스터를 여러 마리 처치하며 몇 판 더 시도
- **"10라운드로 바로가기" 버튼이 안 보임**: 정상입니다 — 테스트용 버튼은 주소 뒤에 `?debug=1`을 붙였을 때만 나타나도록 숨겨져 있습니다
- **모바일에서 드래그해도 기체가 안 움직임**: 게임 화면(캔버스) 밖에서 드래그를 시작했을 가능성이 있습니다. 반드시 캔버스 안쪽에서 손가락을 터치한 채로 움직여야 합니다
- **모바일에서 화면을 회전했는데 크기가 그대로임**: 정상입니다 — 화면 방향에 맞는 해상도는 페이지를 처음 열 때 한 번만 결정되므로, 회전 후에는 새로고침해야 반영됩니다
- **레이저 발키리·드리프터를 선택할 수 없음**: 정상입니다 — 레이저 발키리는 10라운드 최종 보스를 1회 처치해야, 드리프터는 폭탄 아이템을 15회 획득해야 해금됩니다. 로비 캐러셀에서 캐릭터를 넘겨보면 잠금 해제까지 남은 진행률이 표시됩니다
- **결과 화면에서 로비로 돌아가고 싶음**: 실패·엔딩 화면의 `← 로비로` 버튼을 누르면 캐릭터를 다시 선택할 수 있는 로비로 이동합니다
