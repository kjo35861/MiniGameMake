# 검증 안내서 — NEON WARD

> ⚠️ 공개 주소는 GitHub Pages 등에 배포한 뒤 아래 `(배포 주소)` 부분을 실제 URL로 바꿔서 제출하세요.
> 예: `https://kjo35861.github.io/저장소이름/`

## 어디로 가나요
`(배포 주소)` 접속

## 무엇을 하나요
1. 접속하면 나오는 로비 화면에서 **[게임 시작]** 버튼 클릭
2. 방향키(또는 WASD)로 3~5초 정도 움직이며 탄막 피하기
3. 화면 아무 곳이나 클릭할 필요 없이, 하트가 줄거나(피격) 몬스터가 죽는 것(점수 증가)을 확인

## 무엇이 보이면 통과인가요
- 로비 화면에 게임 규칙과 조작법, `게임 시작` 버튼이 보인다
- 시작 후 3초 이내에 화면 상단 좌측 하트(♥♥♥)와 중앙 라운드 진행 바가 정상적으로 보인다
- 방향키를 누르면 기체가 즉시 반응해서 움직이고, 탄막이 화면 위에서 자동으로 발사된다
- 몬스터에 닿거나 총알을 맞으면 하트가 하나 줄고, 하트가 0이 되면 "실패" 화면과 `다시 시작` 버튼이 뜬다
- `다시 시작`을 누르면 라운드 1부터 점수와 체력이 초기화된 새 판이 시작된다
- 2라운드부터 노란 마름모 모양의 속사형 몬스터가 등장해 일정 시간마다 3발씩 끊어 쏜다
- 4라운드부터 남보라색 팔각형 탱커형 몬스터가 등장해 느리게 움직이며 굵은 탄막을 자주 뿌린다
- 몬스터를 처치하면 가끔 노란 폭탄 아이템이 떨어지고, 닿으면 화면의 적 탄이 전부 사라진다
- 진행률 50%에서 엘리트가, 100%에서 보스가 등장하며, 보스를 처치하면 다음 라운드로 넘어간다
- 10라운드의 최종 보스를 처치하면 "GAME CLEAR" 엔딩 화면이 뜬다

## 안될 때
- **화면이 하얗거나 아무것도 안 보임**: 브라우저 새로고침(F5) 후 재시도. 그래도 안 되면 다른 브라우저(Chrome 권장)로 시도
- **방향키를 눌러도 기체가 안 움직임**: 게임 화면(캔버스) 영역을 한 번 클릭해서 포커스를 준 뒤 다시 시도
- **탭을 전환했다가 돌아왔더니 멈춰 있음**: 정상 동작입니다 — 포커스 이탈 시 자동 일시정지되며, 화면의 `계속하기` 버튼을 누르면 이어서 진행됩니다
- **소리가 하나도 안 남**: 상단 `🔊 소리 끄기` 버튼이 눌려있는(음소거) 상태인지 확인
- **폭탄 아이템이 안 보임**: 확률 드롭이라 매 판마다 나오지 않을 수 있음. 몬스터를 여러 마리 처치하며 몇 판 더 시도
- **"10라운드로 바로가기" 버튼이 안 보임**: 정상입니다 — 테스트용 버튼은 주소 뒤에 `?debug=1`을 붙였을 때만 나타나도록 숨겨져 있습니다
