# AI 3줄 — NEON WARD 제작 과정

> 이 파일은 대화 흐름을 바탕으로 작성한 초안입니다. 제출 전에 꼭 한 번 읽어보고
> 본인이 실제로 느낀 부분과 다르면 본인 경험에 맞게 고쳐서 제출하세요.

**AI에게 맡긴 일**
탄막 회피 게임의 전체 코드 작성(HTML/CSS/JS 단일 파일), 자동 발사 무기·웨이브 스폰(일반/속사형/탱커형/엘리트/보스)·
라운드 진행률·하트 체력 시스템·폭탄 픽업 같은 게임 로직 구현, 난이도 수치 조정, 저장/복구 로직과 예외 처리,
색상 테마 변경, 모바일 터치 드래그 조작·세로 화면 해상도 전환, 우측 상단 일시정지 버튼, favicon 파일 생성,
테스트용 버튼의 숨김 처리, 캐릭터 3종(인터셉터·레이저 발키리·드리프터) 무기 로직과 해금 시스템 구현, 로비를
캐러셀 방식으로 재설계, 보스 쫄몹 동반 소환·패턴 5종·직선 레이저·최종 보스 색상 변경, 벽 튕김 탄막, 사용자가
제보한 버그(라운드 클리어 재시작 오류, 모바일 툴팁 화면 밖 이탈, 드리프터 미사일 겹침) 재현·수정, 실제 게임을
헤드리스 브라우저로 구동한 스크린샷 촬영과 자동 검증(콘솔 오류·저장값 손상 복구·해상도별 레이아웃 등), 체크리스트·
검증 안내서·보고서 같은 제출 문서 작성까지 실제 코드와 문서 작업을 전부 맡겼다.

**내가 판단한 일**
1단계 난이도가 지나치게 어렵고 무기가 약하다고 판단해 밸런스 조정을 지시했고, 엘리트 몬스터
등장 방식을 "라운드당 5마리(진행률 15%마다)"에서 "라운드당 1마리(진행률 50%)"로 바꾸도록 규칙을
직접 변경했다. 라운드가 오를수록 몬스터가 더 자주·더 많은 탄을 쏘도록 난이도 곡선을 설계했고,
10라운드를 최종 라운드로 정해 최종 보스 구조를 넣기로 했다. 2~6라운드가 단조롭다고 판단해 AI에게
개선 아이디어를 여러 개 받은 뒤, 그중 "화면 전체 탄 제거 픽업"과 "몬스터 변종 추가" 두 가지만 골라서
적용을 지시했다. 속사형 몬스터의 공격 방식도 처음엔 "연속 발사"로 바꿨다가, 실제로 보니 너무 정신없다고
판단해 다시 "일정 시간마다 3점사"로 최종 조정했다. 네온 테마가 눈에 부담된다고 판단해 저자극 색상으로
바꾸라고 지시했고, 테스트용 단축 버튼은 일반 사용자에게 노출되면 안 된다고 판단해 숨김 처리를 요청했다.
모바일에서 게임 화면이 작아 보인다고 판단해 세로 화면 전용 해상도로 키우도록 지시했고, 조작 편의를 위해
우측 상단에 수동 일시정지 버튼과 `Esc` 키 단축키를 추가하라고 요청했다. 캐릭터 컨셉 3종(확산탄/레이저/유도미사일)을
직접 구상하고 방향을 정했으며, 드리프터는 고화력 대신 체력을 2로 낮춘 "폭발형 딜러" 포지션으로 만들라고
구체적으로 지시했다. 로비 화면이 너무 복잡하다고 판단해 캐러셀 방식으로 단순화하고 규칙은 호버 툴팁으로
옮기라고 요청했다. 보스가 쉽다는 피드백을 받고 쫄몹 동반 소환, 패턴 다양화, 라운드 5 이상 레이저 추가,
최종 보스 색상 변경(진홍색)과 쫄몹 대신 패턴 집중이라는 방향을 직접 정했다. 이후 노란 탄막이 임팩트가
없다고 판단해 벽 튕김과 4초 지속을 요청했고, 실제로 너무 느리다고 느껴 속도를 더 올려달라고 재요청했다.
사용자가 버그(라운드 클리어 후 재시작 시 라운드가 잘못 넘어감, 모바일 툴팁 화면 이탈)를 스크린샷과 함께
제보하면 재현 여부를 판단하고 수정을 지시했다. 몬스터 난이도가 오른 만큼 캐릭터 데미지도 밸런스가
맞지 않는다고 판단해 인터셉터 -40%, 드리프터 -60%로 구체적인 하향 폭을 지정했다.

**AI 말을 안 들은 일**
AI가 지루함 해소 방안으로 픽업 아이템(무적, 점수 2배, 폭탄), 몬스터 변종, 탄막 패턴 변주, 라운드별
서브 목표, 라운드마다 배경색 변화 등 다섯 가지를 제안했는데, 그중 픽업은 "폭탄" 한 종류만, 나머지는
"몬스터 변종"만 채택하고 패턴 변주·서브 목표·배경색 변화는 넣지 않았다. 속사형 몬스터도 AI가 처음
구현한 "총알 없이 몸통 박치기만 하는 버전"과 그다음 "연속 사격 버전"을 둘 다 그대로 쓰지 않고, 최종적으로
"3점사" 방식으로 다시 바꾸게 했다. 드리프터 해금 조건도 AI가 처음 설계한 "누적 처치 150마리"를 그대로
받아들이지 않고 "폭탄 아이템 15회 획득"으로 완전히 다른 조건으로 바꾸게 했다. 벽 튕김 탄막의 속도도
AI가 처음 올린 수치가 여전히 느리다고 판단해 한 번 더 상향을 요청했다.
_(직접 플레이해보면서 추가로 마음에 안 들었던 수치나 구조가 있다면 이어서 적어두세요.)_
