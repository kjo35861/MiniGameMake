# 체크리스트 비교 — NEON WARD (탄막 슈팅 미니게임)

기준: 과제 안내문의 체크리스트. ✅는 통과로 확인된 항목입니다. 아래 5개 항목(3·4·5·6·10·11)은
본인이 직접 플레이하며 최종 확인을 완료했다고 확인해줘서 ✅로 변경했습니다.

| ✓ | # | 체크리스트 항목 | 상태 | 근거 / 확인 방법 |
|---|---|---|---|---|
| ✅ | 1 | 공개 주소에서 한 판을 시작하고 끝낸 뒤 다시 시작할 수 있다 | 구현됨 (코드 검토 완료) | 로비 → `게임 시작` → 실패/보스 처치 → `다시 시작`·`처음부터` 버튼으로 라운드 1부터 재시작. `resetGame()`이 매번 상태 전체 초기화. |
| ✅ | 2 | 핵심 조작과 성공·실패 상태가 화면에서 구분된다 | 구현됨 (코드 검토 완료) | 조작: 방향키/WASD 이동(모바일은 화면 드래그), 공격은 캐릭터별 자동 발사. 상태: `대기중 / 진행중 / 일시정지 / 라운드 클리어 / 최종 클리어 / 실패` 로 구분되어 HUD·오버레이·색상(빨강=실패, 민트=클리어)까지 다르게 표시. 우측 상단 ⏸ 버튼 또는 `Esc` 키로 즉시 일시정지/재개 가능. |
| ✅ | 3 | 두 기준 해상도(1366×768, 1920×1080)에서 조작 영역이 잘리지 않고 가로 넘침이 없다 | 확인 완료 (직접 플레이 검증) | 캔버스가 `max-width: min(900px, 100%)` + `height:auto`로 반응형 구성되어 데스크톱 두 해상도 모두 커버됨. 모바일 세로 화면은 캔버스 내부 해상도를 620×900(세로형)으로 전환해 화면을 꽉 채움. |
| ✅ | 4 | 연속 입력·포커스 이탈·일시정지·10분 실행 검사를 통과했다 | 확인 완료 (직접 플레이 검증) | 연속 입력: `keydown/keyup` 상태값 방식이라 입력 폭주에 안전. 포커스 이탈: `blur`/`visibilitychange` 시 자동 일시정지, ⏸ 버튼 또는 `Esc` 키로 수동 일시정지도 가능. dt는 매 프레임 0.05초로 상한을 둬서 탭 복귀 시 점프 방지. 라운드 클리어 후 2.2초 이내 재시작 시 다음 라운드로 잘못 넘어가던 버그를 타이머 취소 + 상태 가드로 수정하고 재현 테스트로 확인. |
| ✅ | 5 | 콘솔 빨간 오류가 0건이다 | 확인 완료 (직접 플레이 검증) | 이전에 발견됐던 `GET /favicon.ico 404` 오류는 `favicon.ico`·`apple-touch-icon.png` 실제 파일을 추가하고 `<link>`로 연결해 해결. 개발자도구 콘솔 확인 결과 빨간 오류 없음. |
| ✅ | 6 | 변경 전·후 각 10회의 기록이 있고 난이도 값 하나만 바꿨다 | 확인 완료 (직접 플레이 검증) | 난이도 상수는 `NORMAL_SPAWN_INTERVAL_MS`(일반 몬스터 생성 간격, 1700→550ms로 라운드마다 감소)로 화면 하단 정보 패널에도 표시됨. 전/후 10회씩 플레이 기록 비교 완료. |
| ✅ | 7 | 저장값이 비어 있거나 손상돼도 기본값으로 실행된다 | 구현됨 (코드 검토 완료) | `loadSave()`가 `localStorage` 파싱을 `try/catch`로 감싸고, 값 타입 검증(`typeof`, `isFinite`) 후 하나라도 이상하면 `defaultSave()`(best:0, muted:false, reduced:false)로 대체. |
| ✅ | 8 | 현재 판은 초기화되고 보존 대상으로 정한 기록은 유지된다 | 구현됨 (코드 검토 완료) | 새 게임을 시작하면 점수·라운드·체력·무기 레벨은 전부 초기화(`resetGame()`), `localStorage`에 저장된 최고 기록(`save.best`)만 유지됨. |
| ✅ | 9 | 효과를 줄이거나 끄는 선택이 작동한다 | 구현됨 (코드 검토 완료) | `🔊 소리 끄기`: 모든 효과음 재생 함수(`beep`)가 `save.muted` 체크 후 재생. `🌀 움직임 줄이기`: 피격 화면 흔들림(`shake`)과 캔버스 발광 효과(`shadowBlur`)를 즉시 0으로 처리. 두 설정 모두 버튼 클릭 즉시 반영되고 `localStorage`에 저장되어 재접속해도 유지됨. |
| ✅ | 10 | 공개 화면·파일·제출 기록에 개인정보와 비밀값이 0건이다 | 확인 완료 (직접 확인) | 코드 전체에 API 키, 개인정보, 실명 등 없음을 확인. |
| ✅ | 11 | 학생 자체 점검 항목들 모두 완료 | 확인 완료 | 아래 "제출 전 최종 확인 순서" 항목 전체 확인 완료. |

**진행 현황: ✅ 11개 / 11개 (전체 통과)**

## 추가로 구현된 요소 (체크리스트 외 자체 기능)

**캐릭터 시스템 (3종)**
- 인터셉터(기본, 항상 해금) — 확산탄, 레벨업 시 탄이 여러 갈래로 갈라짐
- 레이저 발키리 — 연속 빔 무기(레벨업 시 폭 확장 + 최대 3갈래 분기), **10라운드 최종 보스 1회 처치 시 해금**
- 드리프터 — 유도 미사일(자동 추적, 스플래시 데미지), **최대 체력 2**(다른 캐릭터는 3), **폭탄 아이템 15회 획득 시 해금**. 다중 발사 시 좌/중/우로 발사 지점을 분산해 겹쳐 나가지 않도록 처리
- 로비를 `<캐릭터>` 캐러셀 방식으로 구성, 잠긴 캐릭터는 🔒 표시 + `게임 시작` 버튼 비활성화
- 하단 정보 패널의 규칙 설명을 `?` 버튼 호버/탭 툴팁으로 이동해 로비 화면을 단순화 (모바일에서는 화면 중앙에 고정 표시되도록 처리)

**몬스터 5종**
- 일반 / 속사형(2라운드~, 3점사) / 탱커형(4라운드~, 5-way) / 엘리트(진행률 50%, 1마리) / 보스(진행률 100%)

**보스 난이도 강화**
- 보스 등장 시 쫄몹 동반 소환(엘리트 제외, 티어별 2~4마리) — **10라운드 최종 보스는 쫄몹 없이 패턴 강화로 대체**
- 공격 패턴 5종을 순환 사용: 링버스트 / 조준샷 / 십자탄 / 나선탄(캐스팅마다 회전) / 이중링(최종 보스 전용)
- **5라운드 이상 보스부터 직선 레이저** 추가 — 예고선(0.65초, 실제 피격 판정 폭과 동일하게 표시) → 발사(0.32초) → 재장전
- 최종 보스는 보라색이 아닌 **진홍색(#e0435a)**으로 구분 표시
- 노란 링/나선 탄막은 **벽에 튕기며 4초간 지속**, 속도도 약 60% 증가시켜 위협도를 높임

**데미지 밸런스 조정**
- 인터셉터 데미지 -40%, 드리프터 데미지 -60% (몬스터 난이도 상향에 맞춘 재조정)

**저장/해금 시스템**
- `localStorage`에 최고 기록 + 캐릭터 해금 상태(`laserUnlocked`, `drifterUnlocked`) + 선택 캐릭터(`selectedChar`) + 누적 폭탄 획득 수(`bombsCollected`) 저장
- 손상된 해금 플래그가 있어도 안전하게 `default` 캐릭터로 폴백

**기타**
- 몬스터 처치 시 확률로 💣 폭탄 픽업 드롭 → 획득 시 화면의 적 탄을 전부 제거(충격파 이펙트 포함), 이 획득 횟수가 드리프터 해금 조건으로 연결됨
- 우측 상단 ⏸ 버튼 및 `Esc` 키로 일시정지/재개 토글
- `?debug=1` 파라미터로만 노출되는 "10라운드 바로가기" 테스트용 버튼 (일반 사용자에게는 숨김)
- 네온 대비 저자극 색상 테마, 모바일 드래그 이동 + 세로 전용 해상도(620×900) 전환
- 실제 `favicon.ico` / `apple-touch-icon.png` 파일 추가로 브라우저 자동 요청 404 제거
- 로비 규칙 문구를 실제 동작(라운드 진행률은 몬스터 처치가 아니라 **시간 경과**로 오름)에 맞게 정정

## 이번 개정에서 수정한 버그
- **라운드 클리어 후 재시작 시 다음 라운드로 잘못 넘어가던 문제** — 보스 처치 후 예약된 `setTimeout`이 재시작해도 취소되지 않던 것이 원인. 타이머 id 저장 + `clearTimeout` + 상태 가드로 수정
- **모바일에서 `?` 도움말 툴팁이 화면 밖으로 나가던 문제** — 버튼 기준 정렬 대신 `position:fixed`로 화면 중앙 고정
- **드리프터 미사일 3발이 한 점에서 겹쳐 나가던 문제** — 좌/중/우 발사 오프셋 추가

## 제출 전 최종 확인 순서 (완료)
- [x] 개발자도구 콘솔 켠 채로 로비 → 시작 → 실패 1회, 클리어 1회씩 플레이하며 빨간 오류 확인
- [x] 창 크기를 1366×768, 1920×1080 근처로 바꿔가며 조작 영역이 잘리는지 확인
- [x] 탭을 다른 창으로 전환했다가 돌아와서 자동 일시정지 → 재개가 정상 동작하는지 확인
- [x] 소리 끄기 / 움직임 줄이기 버튼을 각각 켜고 껐을 때 실제로 체감되는지 확인
- [x] 브라우저를 완전히 새로고침한 뒤 최고 기록이 유지되는지, 새 판 점수는 0으로 초기화되는지 확인
- [x] 난이도 상수(`NORMAL_SPAWN_INTERVAL_MS`) 값을 하나 바꿔서 전/후 10회씩 기록 비교
- [x] 2라운드 이후 속사형(3점사), 4라운드 이후 탱커형이 정상적으로 등장·발사하는지, 폭탄 픽업 획득 시 화면 탄이 실제로 사라지는지 확인
- [x] 실제 모바일 기기(또는 브라우저 개발자도구의 기기 시뮬레이션)로 열어서 화면 드래그 이동이 자연스러운지, 세로 화면에서 캔버스가 충분히 크게 보이는지, 배포된 저장소에 `favicon.ico`·`apple-touch-icon.png`가 `index.html`과 함께 루트에 올라가 있는지 확인
- [x] `Esc` 키로 일시정지/재개가 토글되는지 확인
- [x] 레이저 발키리·드리프터가 해금 조건 달성 전에는 선택 불가능하고, 달성 후에는 정상 선택·플레이되는지 확인
- [x] 라운드 클리어 화면에서 곧바로 [처음부터]를 눌러도 라운드가 잘못 넘어가지 않는지 확인
- [x] 모바일 화면에서 `?` 버튼을 눌렀을 때 툴팁이 화면 밖으로 잘리지 않고 중앙에 표시되는지 확인
- [x] 드리프터 무기 레벨이 2발/3발일 때 미사일이 서로 다른 지점에서 발사되는지 확인
